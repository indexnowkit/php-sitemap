<?xml version="1.0"?>
<!--
    Psalm is not part of the regular pipeline (phpstan level 9 is): this configuration exists for the scheduled
    taint analysis of the sitemap reader, the one package that parses untrusted XML fetched from the network.
    Run: psalm with the taint-analysis flag (see .github/workflows/psalm-taint.yml).
-->
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    findUnusedBaselineEntry="false"
    findUnusedCode="false"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <projectFiles>
        <directory name="src" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>
</psalm>
